import {View, Text} from 'react-native';
import React from 'react';

const Chest = () => {
  return <View style={{position: 'absolute', borderLeftWidth: 2, height: '100%'}} />;
};

export default Chest;
